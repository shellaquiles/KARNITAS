# AGENTS.md

1. `.agents/index.json`
2. `core/constraints.md` + `core/directives.md`
3. Feature activa: `specs/<id>/spec.md`
4. SDD: `knowledge/sdd.md` + `workflows/spec_driven.yaml`
5. Plan: `skills/sdd_plan.md` (fase 3)

Roles: specifier → planner → implementer (`agents/`). Sin contexto → detenerse.

Adaptadores por IDE: `adapters/` → instalar con `init-karnitas.sh --adapter <ide>`.
