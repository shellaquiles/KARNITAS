# ADR-002: Multi-agente SDD

**Estado:** Aceptado

Tres roles: **specifier** (spec/clarify) → **planner** (plan/tasks) → **implementer** (código). Handoffs en `agents/`. Orquestación en `workflows/spec_driven.yaml`.
